---
platform: linux


image_resource:
  type: docker-image
  source:
    repository: ubuntu

inputs:
- name: cf-release
- name: postgres-release

run:
  path: postgres-release/ci/scripts/deploy-with-cf/task.sh
