#!/bin/bash -exu
apt-get -y install libdbd-pg-perl
# pre-reqs for s3 support:
apt-get -y install libio-socket-ssl-perl libxml-libxml-perl
source /var/vcap/jobs/pgbackrest/config/config.sh

mkdir -p ${STORE_DIR}
chmod 755 ${STORE_DIR}
chown vcap:vcap ${STORE_DIR}

mkdir -p ${LOG_DIR}
chmod 755 ${LOG_DIR}
chown vcap:vcap ${LOG_DIR}

mkdir -p ${RUN_DIR}
chmod 755 ${RUN_DIR}
chown vcap:vcap ${RUN_DIR}
